#code if you dont have the packages
install.packages("")
#Coca-Cola
statistics_log_rt_ko <- log_rt_ko %>%
summarise(
n_observations = n(),
start = min(Data),
end = max(Data),
min = min(log_retorno, na.rm=TRUE),
max = max(log_retorno, na.rm=TRUE),
q1 = quantile(log_retorno, 0.25, na.rm=TRUE),
mean = mean(log_retorno, na.rm=TRUE),
q3 = quantile(log_retorno, 0.75, na.rm=TRUE),
var = var(log_retorno, na.rm=TRUE),
sd = sd(log_retorno, na.rm=TRUE),
skewness = skewness(log_retorno, na.rm=TRUE),
kurtosis = kurtosis(log_retorno, na.rm=TRUE)
)
#set your working directory
setwd(here::here())
# Create a folder for project-specific libraries, so that we don't need to install the packages
#globally
dir.create("Rlibs", showWarnings = FALSE)
# Create a folder for project-specific libraries, so that we don't need to install the packages
#globally
dir.create("Rlibs", showWarnings = FALSE)
# Create a folder for project-specific libraries, so that we don't need to install the packages
#globally
dir.create("Rlibs", showWarnings = FALSE)
# Install packages in that folder
install.packages(
c("readr", "dplyr", "ggplot2", "moments", "tseries", "randtests", "trend", "nortest","forecast","TTR",
"Kendall","broom" ),
lib = "Rlibs"
)
# Tell R to use that library
.libPaths("Rlibs")
#librarys that are necessary
library(readr)
library(dplyr)
library(moments)
# Install packages in that folder
install.packages("readr", lib = "Rlibs", dependencies = TRUE)
# Install packages in that folder
install.packages(
c("readr", "dplyr", "ggplot2", "moments", "tseries", "randtests", "trend", "nortest","forecast","TTR",
"Kendall","broom"),
lib = "Rlibs",
type = "binary"
)
# Install packages in that folder
install.packages(
c(
"readr", "dplyr", "ggplot2", "moments", "tseries",
"randtests", "trend", "nortest", "forecast", "TTR",
"Kendall", "broom"
),
lib = "Rlibs",
type = "binary"
)
# Install packages in that folder
install.packages(
c(
"readr", "dplyr", "ggplot2", "moments", "tseries",
"randtests", "trend", "nortest", "forecast", "TTR",
"Kendall", "broom"
),
lib = "Rlibs"
)
# Create a folder for project-specific libraries, so that we don't need to install the packages
#globally
dir.create("Rlibs", showWarnings = FALSE)
.libPaths("Rlibs")  # set it as first library path
# List of packages
packages <- c(
"readr","dplyr","ggplot2","moments","tseries",
"randtests","trend","nortest","forecast","TTR",
"Kendall","broom","here"
)
# Install packages (from source, since binaries may not exist)
for(pkg in packages){
if(!require(pkg, character.only = TRUE)){
install.packages(pkg, lib="Rlibs", dependencies = TRUE)
}
library(pkg, character.only = TRUE)
}
